// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.nhd.georgia.application"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
