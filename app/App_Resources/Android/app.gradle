// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "org.nhd.project.assistant.application"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
